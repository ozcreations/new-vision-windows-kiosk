window.__KIOSK_CONFIG = {
  /**
   * Google Maps Platform API key with Places API (New), Static Maps, and Street View Static enabled.
   * Restrict this key to your allowed domains before deploying.
   */
  googleMapsApiKey: 'YOUR_GOOGLE_MAPS_KEY',
};
