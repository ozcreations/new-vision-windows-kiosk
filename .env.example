# (Optional) Environment variables for the legacy Node proxy server.
# The static build now reads the key from config.js instead.
# Replace with your own key if you plan to run the proxy locally.
GOOGLE_MAPS_API_KEY=YOUR_GOOGLE_MAPS_KEY
PORT=4173
